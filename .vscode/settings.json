{
    "typescript.tsdk": "node_modules/typescript/lib",
    "files.exclude": {
        //"**/*.js": { "when": "$(basename).ts" },
        //"**/*.js.map": { "when": "$(basename)"},
        "node_modules": true
    },
    "files.associations": {
        "tslint*.json": "jsonc",
        "config*.json": "jsonc"
    }
}
